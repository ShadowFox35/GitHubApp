name: github_app
description: An application, which searches repositories by name using GitHub API..
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.1.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_native_splash: ^2.2.19

  data:
    path: ./data
  core:
    path: ./core
  core_ui:
    path: ./core_ui/
  domain:
    path: ./domain
  navigation:
    path: ./navigation


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

